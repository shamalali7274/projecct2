<?xml version="1.0" encoding="utf-8"?>
<!--
    الباك ايند حالياً شغال على http (مو https) — laravelBaseUrl بملف
    app_config.dart هو http://192.168.1.102:8000/api. من Android 9
    (API 28) وطالع، النظام يمنع طلبات http (Cleartext) افتراضياً،
    وهاد بالضبط سبب توقف الطلبات بوضع release (بوضع debug/الويب ما
    كان يظهر المنع بنفس الشكل). هاد الملف يسمح بـ http صراحة فقط
    لعنوان السيرفر المحلي.

    ⚠️ لما تصير عندك سيرفر فعلي بدومين وشهادة SSL (https)، بدّلي
    laravelBaseUrl لـ https:// واحذفي/عدّلي هاد الملف، وشيلي
    android:usesCleartextTraffic و android:networkSecurityConfig
    من AndroidManifest.xml.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">192.168.1.102</domain>
        <domain includeSubdomains="true">10.0.2.2</domain>
        <domain includeSubdomains="true">localhost</domain>
        <domain includeSubdomains="true">127.0.0.1</domain>
    </domain-config>
</network-security-config>
